import type { AudioPreset } from '../../interfaces';

import rest from '../../sounds/en/heather/rest.mp3';
import last_one from '../../sounds/en/heather/last_one.mp3';
import training_completed from '../../sounds/en/heather/training_completed.mp3';
import work from '../../sounds/en/heather/work.mp3';

export const heatherAudioPreset: AudioPreset = {
  rest,
  last_one,
  training_completed,
  work
};
